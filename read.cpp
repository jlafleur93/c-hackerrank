#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

//Basic program to read user input and output the sum of the user input for hackerrank
int main() {
    int s, n , z;
    cin >> s >> n >> z; 
    cout << s + n + z << endl;
    return 0;
}
